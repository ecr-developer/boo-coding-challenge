plugins {
    id 'java-conventions'
    id 'java-library'
}

group 'boo.ecrodrigues.admin.user.application'

dependencies {
    implementation(project(":domain"))

    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}