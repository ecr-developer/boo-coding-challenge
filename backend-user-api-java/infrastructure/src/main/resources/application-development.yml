logging:
  level:
    ROOT: DEBUG
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG

server:
  port: 8080
  undertow:
    threads:
      worker: 12
      io: 4

spring:
  devtools:
    restart:
      enabled: false

swaggerBaseUrl: http://localhost:8080